<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="co.revely.gradient">

	<!-- To auto-complete the email text field in the login form with the user's emails -->
	<uses-permission android:name="android.permission.GET_ACCOUNTS" />
	<uses-permission android:name="android.permission.READ_PROFILE" />
	<uses-permission android:name="android.permission.READ_CONTACTS" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">

		<activity android:name=".HomeActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity
			android:name=".BackgroundGradientActivity"
			android:theme="@style/AppThemeBackgroundGradient"
			/>

		<activity
			android:name=".TextGradientActivity"
			android:theme="@style/AppThemeTextGradient"
			/>

		<activity
			android:name=".AnimatedGradientActivity"
			android:theme="@style/AppThemeAnimatedGradient"
			/>

		<activity
			android:name=".OtherGradientActivity"
			android:theme="@style/AppThemeOtherGradient"
			/>

		<activity
			android:name=".JavaGradientActivity"
			/>

		<activity
			android:name=".LayerGradientActivity"
			/>
	</application>

</manifest>